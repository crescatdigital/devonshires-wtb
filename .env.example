# Supabase (not used yet — the CMS is currently a static JSON file).
# When we switch the CMS to Supabase, copy this file to .env.local and fill in
# the values. Never commit .env.local.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
